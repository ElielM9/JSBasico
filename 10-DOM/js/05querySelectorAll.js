// Seleccionar elemmentos con queryselectorall
const cards = document.querySelectorAll(`.card`); // Selecciona todos los elementos con la misma clase
console.log(cards);

const existe = document.querySelectorAll(`existe`); // Regresa un nodelist vacio
console.log(existe);