// ForEach y Map en JS
const pendientes = ["Tarea", "Comer", "Trabajar"];

// ForEach
pendientes.forEach((pendiente, index) => {
  console.log(`${index}: ${pendiente}`);
});

// Map
const nuevoArray = pendientes.map((pendiente) => {
  pendiente;
});

console.log(nuevoArray);